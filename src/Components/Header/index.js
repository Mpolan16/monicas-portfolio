import React from 'react';
import { Col, Row, Container } from "../Grid";

function Header(props) {
    return (
        <Container fluid>
        <Row>
          <Col size="sm-12">
          </Col>
        </Row>
      </Container>
    );
}

export default Header;